#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <stdint.h>
// #include "../syscalls/syscalls.h"

int main()
{
    printf("Ok ok\n");

    // puts("Dono");

    // int i = 5;

    // const char *message = "Hello World\n";
    // size_t message_length = 12; // Length of the message, including the newline character

    // asm volatile("xchg %bx, %bx");

    // Call the write function
    // write(1, message, message_length);

    for (;;)
    {
    }
}
